export async function getAIReply(prompt) {
  // Stubbed AI response for the first project version.
  // Replace this with Claude API integration later.
  return Promise.resolve(`(AI stub) Received prompt: ${prompt}`);
}
