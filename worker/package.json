{
  "name": "plot-point-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/providers/openaiProvider.js && node --check src/schemas/characterSchema.js && node --check src/schemas/sceneSchema.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "openai": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
